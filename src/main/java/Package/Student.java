package Package;

import java.util.ArrayList;
import java.util.List;

public class Student {
    public static void main(String[] args) {
        String name, email;
        int id, bonus, report, app;
        float lt, totalpoint;

        //totalpoint = ((bonus*10/100) + (report*30/100) + (app*15/100) + (lt*45/100) );

    }
}
